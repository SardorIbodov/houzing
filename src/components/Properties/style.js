import { styled } from "styled-components";

const Container = styled.div`
  /* max-width: 1440px; */
  display: flex;
  flex-wrap: wrap;
  padding: var(--padding);
  gap: 20px;
  justify-content: space-between;
`;

export { Container };
